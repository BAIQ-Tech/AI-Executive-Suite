# Green environment configuration (app4, app5, app6)
upstream app_servers {
    least_conn;
    server app4:5000 max_fails=3 fail_timeout=30s;
    server app5:5000 max_fails=3 fail_timeout=30s;
    server app6:5000 max_fails=3 fail_timeout=30s;
    keepalive 32;
}